/** Temporary visibility while the replacement Hero is being designed.
 *  Keep the original components/assets intact; set a flag to true to restore.
 *  These flags affect presentation only, not the commercial offer.
 */
export const pageSections = {
  hero: false,
  booklet: false,
} as const;
